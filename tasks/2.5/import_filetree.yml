---
- name: Include FileTree Common Variables for AAP {{ casc_aap_version }}
  ansible.builtin.include_vars:
    file: "{{ playbook_dir }}/common_vars/vars_filetree_common.yml"
  tags:
    - always

- name: FileTree Read for AAP {{ casc_aap_version }}
  ansible.builtin.import_role:
    name: infra.aap_configuration_extended.filetree_read
  tags:
    - always
...
